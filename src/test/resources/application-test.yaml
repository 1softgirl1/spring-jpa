# src/test/resources/application-test.yaml
spring:
  datasource:
    url: jdbc:tc:postgresql:17:///testdb
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    root: WARN
    com.example.delivery: DEBUG