info:
  name: get_by_id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/restaurants/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
