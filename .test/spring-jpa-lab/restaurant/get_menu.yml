info:
  name: get_menu
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/restaurants/2/dishes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
