info:
  name: get1
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/dishes?namePart=Pizza"
  params:
    - name: namePart
      value: Pizza
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
