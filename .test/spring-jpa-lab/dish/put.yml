info:
  name: put
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/dishes/2"
  body:
    type: json
    data: |-
      {
        "name": "string",
        "description": "string",
        "price": 10,
        "isAvailable": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
